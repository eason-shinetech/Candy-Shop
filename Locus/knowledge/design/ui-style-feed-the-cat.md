---
id: kd_2f92b1b5-8488-43a4-95bc-5b1648868dfb
injectMode: inherit
aiEditMode: inherit
---

# UI 风格方向：Feed the cat puzzle game（Behance 198313043）

用户提供的参考截图确定的 UI 改造方向。素材保持项目现有粉色糖果风 PNG 不变，只调整布局贴近参考图。

## 参考风格规范（如后续需要生成/替换素材时使用）

- 配色：背景浅粉 `#F9CBD6` / 浅绿 `#DDE8C4`；面板薰衣草紫 `#A9A3E8`（深紫描边 `#6C63C7`）；卡片奶油色 `#FFF3D9`（深棕描边 `#5B3A29`）；点缀珊瑚红 `#F26D6D`、成功绿 `#7ED957`、金币黄 `#F7B32B`
- 形状：大圆角（卡片约 24px）、统一 3~4px 深棕描边、扁平无光泽、顶部缎带横幅标题
- 字体：粗圆体（项目内对应 `Assets/Art/Fonts/MaoKenZhuYuanTi`），正文深棕 `#6B3F2A`，缎带上奶油色描边字

## 已实施的布局映射（2026-08，用户确认）

- MainMenu：金币+体力胶囊左上横排；商店/设置为右上圆形按钮（ui_rounded 奶油色调，商店用 icon_recipe_book，设置用文字）；中央 panel_cream 大卡片（标题/副标题/最佳成绩/开始营业按钮在卡内）；ChallengeBanner 为卡片下方窄胶囊条；Streak 圆点底部居中
- GameHUD：顶栏从左到右 = 星星、体力胶囊(300宽)、金币胶囊(CoinsPill, ui_rounded)、圆形暂停按钮；底部道具按钮缩小为 240x150 靠左下横排；弹窗标题统一移到面板顶部横幅位
- RecipeShop：金币胶囊左上，返回按钮左下
- 注意：Top 子节点用边缘锚点（左列锚 (0,0.5) 用正坐标，右侧锚 (1,0.5) 用负坐标），不要用居中坐标

## 待用户验证

弹窗标题新位置、道具按钮缩小后的手感（可点性）需 Play Mode 实测。
